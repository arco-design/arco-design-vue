---
title: 下拉菜单 Dropdown
description: 页面上的命令过多时，可将备选命令收纳到向下展开的浮层容器中。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import DropdownBasicDemo from '../../../../components/generated/dropdown/basic.vue';
import dropdownBasicSource from '../../../../components/generated/dropdown/basic.vue?raw';
import DropdownButtonDemo from '../../../../components/generated/dropdown/button.vue';
import dropdownButtonSource from '../../../../components/generated/dropdown/button.vue?raw';
import DropdownContextMenuDemo from '../../../../components/generated/dropdown/context-menu.vue';
import dropdownContextMenuSource from '../../../../components/generated/dropdown/context-menu.vue?raw';
import DropdownGroupDemo from '../../../../components/generated/dropdown/group.vue';
import dropdownGroupSource from '../../../../components/generated/dropdown/group.vue?raw';
import DropdownIconDemo from '../../../../components/generated/dropdown/icon.vue';
import dropdownIconSource from '../../../../components/generated/dropdown/icon.vue?raw';
import DropdownPositionDemo from '../../../../components/generated/dropdown/position.vue';
import dropdownPositionSource from '../../../../components/generated/dropdown/position.vue?raw';
import DropdownSubmenuDemo from '../../../../components/generated/dropdown/submenu.vue';
import dropdownSubmenuSource from '../../../../components/generated/dropdown/submenu.vue?raw';
import DropdownTriggerDemo from '../../../../components/generated/dropdown/trigger.vue';
import dropdownTriggerSource from '../../../../components/generated/dropdown/trigger.vue?raw';

### 基本用法

下拉菜单的基本用法。下拉菜单开启后会为触发元素添加 `sd-dropdown-open` 类名。

<DemoBlock source={dropdownBasicSource} mainFile="src/dropdown-basic.vue">

<DropdownBasicDemo client:only="vue" />

</DemoBlock>

### 按钮下拉框

可以使用 `<dropdown-button>` 组件用来展示右边是额外相关功能菜单的按钮。 `2.16.0` 版本添加支持。

<DemoBlock source={dropdownButtonSource} mainFile="src/dropdown-button.vue">

<DropdownButtonDemo client:only="vue" />

</DemoBlock>

### 右键菜单

移入区域后，可点击鼠标右键触发。

<DemoBlock source={dropdownContextMenuSource} mainFile="src/dropdown-context-menu.vue">

<DropdownContextMenuDemo client:only="vue" />

</DemoBlock>

### 分组选项

通过 `<dgroup>` 组件使用分组选项。

<DemoBlock source={dropdownGroupSource} mainFile="src/dropdown-group.vue">

<DropdownGroupDemo client:only="vue" />

</DemoBlock>

### 带图标的选项

通过 `icon` 插槽在选项前添加图标。

<DemoBlock source={dropdownIconSource} mainFile="src/dropdown-icon.vue">

<DropdownIconDemo client:only="vue" />

</DemoBlock>

### 弹出方向

通过 `position` 支持指定 6 种弹出方位，分别是：top: 向上, tl: 左上, tr: 右上, bottom: 下方(默认), bl: 左下, br: 右下。

<DemoBlock source={dropdownPositionSource} mainFile="src/dropdown-position.vue">

<DropdownPositionDemo client:only="vue" />

</DemoBlock>

### 多级菜单

带有多级菜单的下拉框。

<DemoBlock source={dropdownSubmenuSource} mainFile="src/dropdown-submenu.vue">

<DropdownSubmenuDemo client:only="vue" />

</DemoBlock>

### 触发方式

通过 `trigger` 指定触发方式。

<DemoBlock source={dropdownTriggerSource} mainFile="src/dropdown-trigger.vue">

<DropdownTriggerDemo client:only="vue" />

</DemoBlock>

`<dropdown>` 组件继承 `<trigger>` 组件的全部属性

## API

### `<dropdown>` Props

| 参数名 | 描述 | 类型 | 默认值 | 版本 |
| --- | --- | --- | :-: | :-- |
| popup-visible **(v-model)** | 弹出框是否可见 | `boolean` | `-` |  |
| default-popup-visible | 弹出框默认是否可见（非受控模式） | `boolean` | `false` |  |
| trigger | 触发方式 | `'hover' \| 'click' \| 'focus' \| 'contextMenu'` | `'click'` |  |
| position | 弹出位置 | `'top' \| 'tl' \| 'tr' \| 'bottom' \| 'bl' \| 'br'` | `'bottom'` |  |
| popup-container | 弹出框的挂载容器 | `string \| HTMLElement` | `-` |  |
| popup-max-height | 弹出框最大高度 | `boolean\|number` | `true` | 2.29.0 |
| hide-on-select | 是否在用户选择后隐藏弹出框 | `boolean` | `true` | 2.43.0 |

### `<dropdown>` Events

| 事件名 | 描述 | 参数 |
| --- | --- | --- |
| popup-visible-change | 下拉框显示状态发生改变时触发 | visible: `boolean` |
| select | 用户选择时触发 | value: `string \| number \| Record<string, any> \| undefined `<br />ev: `Event` |

### `<dropdown>` Slots

| 插槽名  | 描述 | 参数 | 版本   |
| ------- | :--: | ---- | :----- |
| content | 内容 | -    |        |
| footer  | 页脚 | -    | 2.10.0 |

### `<doption>` Props

| 参数名   | 描述     | 类型                     | 默认值  |
| -------- | -------- | ------------------------ | :-----: |
| value    | 选项值   | `string\|number\|object` |   `-`   |
| disabled | 是否禁用 | `boolean`                | `false` |

### `<doption>` Events

| 事件名 | 描述           | 参数             |
| ------ | -------------- | ---------------- |
| click  | 点击按钮时触发 | ev: `MouseEvent` |

### `<doption>` Slots

| 插槽名 | 描述 | 参数 |
| ------ | :--: | ---- |
| icon   | 图标 | -    |

### `<dgroup>` Props

| 参数名 | 描述     | 类型     | 默认值 |
| ------ | -------- | -------- | :----: |
| title  | 分组标题 | `string` |  `-`   |

### `<dgroup>` Slots

| 插槽名 |   描述   | 参数 | 版本   |
| ------ | :------: | ---- | :----- |
| title  | 分组标题 | -    | 2.10.0 |

### `<dsubmenu>` Props

| 参数名 | 描述 | 类型 | 默认值 | 版本 |
| --- | --- | --- | :-: | :-- |
| value | 选项值（2.16.0 版本后暂无用处） | `string\|number` | `-` |  |
| disabled | 是否禁用 | `boolean` | `false` | 2.10.0 |
| trigger | 触发方式 | `'hover' \| 'click'` | `'click'` | 2.10.0 |
| position | 弹出位置 | `'rt' \| 'lt'` | `'rt'` | 2.10.0 |
| popup-visible **(v-model)** | 弹出框是否可见 | `boolean` | `-` |  |
| default-popup-visible | 弹出框默认是否可见（非受控模式） | `boolean` | `false` |  |
| option-props | 自定义选项属性 | `object` | `-` | 2.29.0 |

### `<dsubmenu>` Events

| 事件名               | 描述                         | 参数               |
| -------------------- | ---------------------------- | ------------------ |
| popup-visible-change | 下拉框显示状态发生改变时触发 | visible: `boolean` |

### `<dsubmenu>` Slots

| 插槽名  |    描述    | 参数 | 版本   |
| ------- | :--------: | ---- | :----- |
| icon    |    图标    | -    | 2.29.0 |
| content | 子菜单内容 | -    |        |
| footer  |    页脚    | -    | 2.10.0 |

### `<dropdown-button>` Props

| 参数名 | 描述 | 类型 | 默认值 |
| --- | --- | --- | :-: |
| popup-visible **(v-model)** | 弹出框是否可见 | `boolean` | `-` |
| default-popup-visible | 弹出框默认是否可见（非受控模式） | `boolean` | `false` |
| trigger | 触发方式 | `'hover' \| 'click' \| 'focus' \| 'contextMenu'` | `'click'` |
| position | 弹出位置 | `'top' \| 'tl' \| 'tr' \| 'bottom' \| 'bl' \| 'br'` | `'br'` |
| popup-container | 弹出框的挂载容器 | `string \| HTMLElement` | `-` |
| disabled | 是否禁用 | `boolean` | `false` |
| type | 按钮类型 | `string` | `-` |
| size | 按钮大小 | `string` | `-` |
| button-props | 按钮属性 | `ButtonProps` | `-` |
| hide-on-select | 是否在用户选择后隐藏弹出框 | `boolean` | `true` |

### `<dropdown-button>` Events

| 事件名 | 描述 | 参数 |
| --- | --- | --- |
| popup-visible-change | 下拉框显示状态发生改变时触发 | visible: `boolean` |
| click | 点击按钮时触发 | ev: `MouseEvent` |
| select | 用户选择时触发 | value: `string \| number \| Record<string, any> \| undefined`<br />ev: `Event` |

### `<dropdown-button>` Slots

| 插槽名  |   描述   | 参数                    |
| ------- | :------: | ----------------------- |
| icon    | 按钮图标 | popupVisible: `boolean` |
| content |   内容   | -                       |
