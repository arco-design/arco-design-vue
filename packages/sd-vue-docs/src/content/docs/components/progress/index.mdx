---
title: 进度条 Progress
description: 给予用户当前系统执行中任务运行状态的反馈，多用于运行一段时间的场景，有效减轻用户在等待中产生的焦虑感。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import ProgressBasicDemo from '../../../../components/generated/progress/basic.vue';
import progressBasicSource from '../../../../components/generated/progress/basic.vue?raw';
import ProgressCircleDemo from '../../../../components/generated/progress/circle.vue';
import progressCircleSource from '../../../../components/generated/progress/circle.vue?raw';
import ProgressLinearDemo from '../../../../components/generated/progress/linear.vue';
import progressLinearSource from '../../../../components/generated/progress/linear.vue?raw';
import ProgressMiniDemo from '../../../../components/generated/progress/mini.vue';
import progressMiniSource from '../../../../components/generated/progress/mini.vue?raw';
import ProgressSizeDemo from '../../../../components/generated/progress/size.vue';
import progressSizeSource from '../../../../components/generated/progress/size.vue?raw';
import ProgressStatusDemo from '../../../../components/generated/progress/status.vue';
import progressStatusSource from '../../../../components/generated/progress/status.vue?raw';
import ProgressStepsDemo from '../../../../components/generated/progress/steps.vue';
import progressStepsSource from '../../../../components/generated/progress/steps.vue?raw';
import ProgressTrackcolorDemo from '../../../../components/generated/progress/trackcolor.vue';
import progressTrackcolorSource from '../../../../components/generated/progress/trackcolor.vue?raw';

### 基本用法

简单的进度条。

<DemoBlock source={progressBasicSource} mainFile="src/progress-basic.vue">

<ProgressBasicDemo client:only="vue" />

</DemoBlock>

### 环形进度条

设置 `type="circle"` 将会展示环形进度条。

<DemoBlock source={progressCircleSource} mainFile="src/progress-circle.vue">

<ProgressCircleDemo client:only="vue" />

</DemoBlock>

### 渐变进度条

`color` 传入对象时， 会作为 `linear-gradient` 的属性值设置渐变色。

<DemoBlock source={progressLinearSource} mainFile="src/progress-linear.vue">

<ProgressLinearDemo client:only="vue" />

</DemoBlock>

### 迷你进度条

设置 `size="mini"` 展示微型进度条。

<DemoBlock source={progressMiniSource} mainFile="src/progress-mini.vue">

<ProgressMiniDemo client:only="vue" />

</DemoBlock>

### 进度条大小

通过 `size` 设置进度条的大小

<DemoBlock source={progressSizeSource} mainFile="src/progress-size.vue">

<ProgressSizeDemo client:only="vue" />

</DemoBlock>

### 进度条状态

通过 `status` 指定进度条状态

<DemoBlock source={progressStatusSource} mainFile="src/progress-status.vue">

<ProgressStatusDemo client:only="vue" />

</DemoBlock>

### 步骤进度条

通过设置 `steps` 展示步骤进度条。

<DemoBlock source={progressStepsSource} mainFile="src/progress-steps.vue">

<ProgressStepsDemo client:only="vue" />

</DemoBlock>

### 剩余进度条的颜色

可以通过 trackColor 设置剩余进度条的颜色

<DemoBlock source={progressTrackcolorSource} mainFile="src/progress-trackcolor.vue">

<ProgressTrackcolorDemo client:only="vue" />

</DemoBlock>

## API

### `<progress>` Props

| 参数名 | 描述 | 类型 | 默认值 |
| --- | --- | --- | :-: |
| type | 进度条的类型 | `'line' \| 'circle'` | `'line'` |
| size | 进度条的大小 | `'mini' \| 'small' \| 'medium' \| 'large'` | `'medium'` |
| percent | 进度条当前的百分比 | `number` | `0` |
| steps | 开启步骤条模式，并设置步骤数 | `number` | `0` |
| animation | 是否开启过渡动画 | `boolean` | `false` |
| stroke-width | 进度条的线宽 | `number` | `-` |
| width | 进度条的长度 | `number\|string` | `-` |
| color | 进度条的颜色 | `string\|object` | `-` |
| track-color | 进度条的轨道颜色 | `string` | `-` |
| show-text | 是否显示文字 | `boolean` | `true` |
| status | 进度条状态 | `'normal' \| 'success' \| 'warning' \| 'danger'` | `-` |
