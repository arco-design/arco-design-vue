---
title: 加载中 Spin
description: 用于页面和区块的加载中状态 - 页面局部处于等待异步数据或正在渲染过程时，合适的加载动效会有效缓解用户的焦虑。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import SpinBasicDemo from '../../../../components/generated/spin/basic.vue';
import spinBasicSource from '../../../../components/generated/spin/basic.vue?raw';
import SpinContainerDemo from '../../../../components/generated/spin/container.vue';
import spinContainerSource from '../../../../components/generated/spin/container.vue?raw';
import SpinDotDemo from '../../../../components/generated/spin/dot.vue';
import spinDotSource from '../../../../components/generated/spin/dot.vue?raw';
import SpinIconDemo from '../../../../components/generated/spin/icon.vue';
import spinIconSource from '../../../../components/generated/spin/icon.vue?raw';
import SpinSizeDemo from '../../../../components/generated/spin/size.vue';
import spinSizeSource from '../../../../components/generated/spin/size.vue?raw';
import SpinTipDemo from '../../../../components/generated/spin/tip.vue';
import spinTipSource from '../../../../components/generated/spin/tip.vue?raw';

### 基本用法

用于展示加载中的状态。

<DemoBlock source={spinBasicSource} mainFile="src/spin-basic.vue">

<SpinBasicDemo client:only="vue" />

</DemoBlock>

### 容器中

可以给任意内容添加加载中指示符。

<DemoBlock source={spinContainerSource} mainFile="src/spin-container.vue">

<SpinContainerDemo client:only="vue" />

</DemoBlock>

### 点类型指示符

通过 `dot` 属性，可以展示点类型的指示符。

<DemoBlock source={spinDotSource} mainFile="src/spin-dot.vue">

<SpinDotDemo client:only="vue" />

</DemoBlock>

### 自定义图标

通过 `#icon` 插槽可以自定义图标。

<DemoBlock source={spinIconSource} mainFile="src/spin-icon.vue">

<SpinIconDemo client:only="vue" />

</DemoBlock>

### 不同尺寸

设置 `size` 可以得到不同尺寸的加载图标。

<DemoBlock source={spinSizeSource} mainFile="src/spin-size.vue">

<SpinSizeDemo client:only="vue" />

</DemoBlock>

### 添加描述文案

通过 `tip` 属性添加描述文案。

<DemoBlock source={spinTipSource} mainFile="src/spin-tip.vue">

<SpinTipDemo client:only="vue" />

</DemoBlock>

## API

### `<spin>` Props

| 参数名    | 描述                                   | 类型      | 默认值  |
| --------- | -------------------------------------- | --------- | :-----: |
| size      | 尺寸                                   | `number`  |   `-`   |
| loading   | 是否为加载中状态（仅在容器模式下生效） | `boolean` | `false` |
| dot       | 是否使用点类型的动画                   | `boolean` | `false` |
| tip       | 提示内容                               | `string`  |   `-`   |
| hide-icon | 是否隐藏图标                           | `boolean` | `false` |

### `<spin>` Slots

| 插槽名  |          描述          | 参数 |
| ------- | :--------------------: | ---- |
| tip     |     自定义提示内容     | -    |
| element |       自定义元素       | -    |
| icon    | 自定义图标（自动旋转） | -    |
