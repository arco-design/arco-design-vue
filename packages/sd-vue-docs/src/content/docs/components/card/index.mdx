---
title: 卡片 Card
description: 将信息分类后分标题、详情等区域聚合展现，一般作为简洁介绍或者信息的大盘和入口。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import CardActionsDemo from '../../../../components/generated/card/actions.vue';
import cardActionsSource from '../../../../components/generated/card/actions.vue?raw';
import CardBasicDemo from '../../../../components/generated/card/basic.vue';
import cardBasicSource from '../../../../components/generated/card/basic.vue?raw';
import CardBorderedDemo from '../../../../components/generated/card/bordered.vue';
import cardBorderedSource from '../../../../components/generated/card/bordered.vue?raw';
import CardContentDemo from '../../../../components/generated/card/content.vue';
import cardContentSource from '../../../../components/generated/card/content.vue?raw';
import CardGridDemo from '../../../../components/generated/card/grid.vue';
import cardGridSource from '../../../../components/generated/card/grid.vue?raw';
import CardHoverableDemo from '../../../../components/generated/card/hoverable.vue';
import cardHoverableSource from '../../../../components/generated/card/hoverable.vue?raw';
import CardInnerDemo from '../../../../components/generated/card/inner.vue';
import cardInnerSource from '../../../../components/generated/card/inner.vue?raw';
import CardMetaDemo from '../../../../components/generated/card/meta.vue';
import cardMetaSource from '../../../../components/generated/card/meta.vue?raw';
import CardRowDemo from '../../../../components/generated/card/row.vue';
import cardRowSource from '../../../../components/generated/card/row.vue?raw';

### 支持更多内容配置

`actions` slot 可以用于展示底部按钮组。

<DemoBlock source={cardActionsSource} mainFile="src/card-actions.vue">

<CardActionsDemo client:only="vue" />

</DemoBlock>

### 基本用法

常规的内容容器，可承载文字、列表、图片、段落，常用于模块划分和内容概览。

<DemoBlock source={cardBasicSource} mainFile="src/card-basic.vue">

<CardBasicDemo client:only="vue" />

</DemoBlock>

### 无边框卡片

设置 `bordered` 为 `false` 来使用无边框卡片。

<DemoBlock source={cardBorderedSource} mainFile="src/card-bordered.vue">

<CardBorderedDemo client:only="vue" />

</DemoBlock>

### 简洁卡片

卡片可以只有内容区域。

<DemoBlock source={cardContentSource} mainFile="src/card-content.vue">

<CardContentDemo client:only="vue" />

</DemoBlock>

### 网络型内嵌卡片

通过 `Card.Grid` 来使用卡片内容区隔模式。

<DemoBlock source={cardGridSource} mainFile="src/card-grid.vue">

<CardGridDemo client:only="vue" />

</DemoBlock>

### 鼠标悬浮样式

指定 `hoverable` 来为卡片添加鼠标悬浮样式，同时你可以通过样式覆盖来自定义悬浮样式。

<DemoBlock source={cardHoverableSource} mainFile="src/card-hoverable.vue">

<CardHoverableDemo client:only="vue" />

</DemoBlock>

### 内部卡片

卡片中可以嵌套其他卡片组件。

<DemoBlock source={cardInnerSource} mainFile="src/card-inner.vue">

<CardInnerDemo client:only="vue" />

</DemoBlock>

### 更灵活的内容展示

使用 `Card.Meta` 支持更加灵活的内容（封面、头像、 标题、描述信息）

<DemoBlock source={cardMetaSource} mainFile="src/card-meta.vue">

<CardMetaDemo client:only="vue" />

</DemoBlock>

### 栅格卡片

在系统概览页面常常和栅格进行配合。

<DemoBlock source={cardRowSource} mainFile="src/card-row.vue">

<CardRowDemo client:only="vue" />

</DemoBlock>

## API

### `<card>` Props

| 参数名       | 描述                 | 类型                  |    默认值    |
| ------------ | -------------------- | --------------------- | :----------: |
| bordered     | 是否有边框           | `boolean`             |    `true`    |
| loading      | 是否为加载中         | `boolean`             |   `false`    |
| hoverable    | 是否可悬浮           | `boolean`             |   `false`    |
| size         | 卡片尺寸             | `'medium' \| 'small'` |  `'medium'`  |
| header-style | 自定义标题区域样式   | `CSSProperties`       | `() => ({})` |
| body-style   | 内容区域自定义样式   | `CSSProperties`       | `() => ({})` |
| title        | 卡片标题             | `string`              |     `-`      |
| extra        | 卡片右上角的操作区域 | `string`              |     `-`      |

### `<card>` Slots

| 插槽名  |         描述         | 参数 |
| ------- | :------------------: | ---- |
| actions |   卡片底部的操作组   | -    |
| cover   |       卡片封面       | -    |
| extra   | 卡片右上角的操作区域 | -    |
| title   |       卡片标题       | -    |

### `<card-meta>` Props

| 参数名      | 描述 | 类型     | 默认值 |
| ----------- | ---- | -------- | :----: |
| title       | 标题 | `string` |  `-`   |
| description | 描述 | `string` |  `-`   |

### `<card-meta>` Slots

| 插槽名      | 描述 | 参数 |
| ----------- | :--: | ---- |
| description | 描述 | -    |
| title       | 标题 | -    |
| avatar      | 头像 | -    |

### `<card-grid>` Props

| 参数名    | 描述         | 类型      | 默认值  |
| --------- | ------------ | --------- | :-----: |
| hoverable | 是否可以悬浮 | `boolean` | `false` |
