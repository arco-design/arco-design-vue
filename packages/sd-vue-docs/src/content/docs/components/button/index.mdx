---
title: 按钮 Button
description: 按钮是一种命令组件，可发起一个即时操作。
---

import DemoBlock from "../../../../components/docs/DemoBlock.astro";
import ButtonBasicDemo from "../../../../components/generated/button/basic.vue";
import buttonBasicSource from "../../../../components/generated/button/basic.vue?raw";
import ButtonDisabledDemo from "../../../../components/generated/button/disabled.vue";
import buttonDisabledSource from "../../../../components/generated/button/disabled.vue?raw";
import ButtonGroupDemo from "../../../../components/generated/button/group.vue";
import buttonGroupSource from "../../../../components/generated/button/group.vue?raw";
import ButtonIconDemo from "../../../../components/generated/button/icon.vue";
import buttonIconSource from "../../../../components/generated/button/icon.vue?raw";
import ButtonLoadingDemo from "../../../../components/generated/button/loading.vue";
import buttonLoadingSource from "../../../../components/generated/button/loading.vue?raw";
import ButtonLongDemo from "../../../../components/generated/button/long.vue";
import buttonLongSource from "../../../../components/generated/button/long.vue?raw";
import ButtonShapeDemo from "../../../../components/generated/button/shape.vue";
import buttonShapeSource from "../../../../components/generated/button/shape.vue?raw";
import ButtonSizeDemo from "../../../../components/generated/button/size.vue";
import buttonSizeSource from "../../../../components/generated/button/size.vue?raw";
import ButtonStatusDemo from "../../../../components/generated/button/status.vue";
import buttonStatusSource from "../../../../components/generated/button/status.vue?raw";

### 基本用法

按钮分为 `primary` - **主要按钮**、`secondary` - **次要按钮（默认）**、`dashed` - **虚线按钮**、`outline` - **线形按钮**、`text` - **文本按钮**五种类型。

<DemoBlock source={buttonBasicSource} mainFile="src/button-basic.vue">

<ButtonBasicDemo client:only="vue" />

</DemoBlock>

### 禁用状态

按钮的禁用状态。

<DemoBlock source={buttonDisabledSource} mainFile="src/button-disabled.vue">

<ButtonDisabledDemo client:only="vue" />

</DemoBlock>

### 组合按钮

通过 `<sd-button-group>` 组件使按钮以组合方式出现。可用在同级多项操作中。

<DemoBlock source={buttonGroupSource} mainFile="src/button-group.vue">

<ButtonGroupDemo client:only="vue" />

</DemoBlock>

### 图标按钮

按钮可以嵌入图标。在只设置图标时，按钮的宽高相等。

<DemoBlock source={buttonIconSource} mainFile="src/button-icon.vue">

<ButtonIconDemo client:only="vue" />

</DemoBlock>

### 加载中状态

通过设置 `loading` 可以让按钮处于加载中状态。处于加载中状态的按钮不会触发点击事件。

<DemoBlock source={buttonLoadingSource} mainFile="src/button-loading.vue">

<ButtonLoadingDemo client:only="vue" />

</DemoBlock>

### 长按钮

通过设置 `long` 属性，使按钮的宽度跟随容器的宽度。

<DemoBlock source={buttonLongSource} mainFile="src/button-long.vue">

<ButtonLongDemo client:only="vue" />

</DemoBlock>

### 按钮形状

按钮分为 `square` - **长方形（默认）**、`circle` - **圆形**、`round` - **全圆角**三种形状。

<DemoBlock source={buttonShapeSource} mainFile="src/button-shape.vue">

<ButtonShapeDemo client:only="vue" />

</DemoBlock>

### 按钮尺寸

按钮分为 `mini`、`small`、`medium`、`large` 四种尺寸。高度分别为：`24px`、`28px`、`32px`、`36px`。推荐（默认）尺寸为 `medium`。可在不同场景及不同业务需求选择适合尺寸。

<DemoBlock source={buttonSizeSource} mainFile="src/button-size.vue">

<ButtonSizeDemo client:only="vue" />

</DemoBlock>

### 按钮状态

按钮的状态分为 `normal` - **正常（默认）**、`success` - **成功**、`warning` - **警告**、`danger` - **危险**四种，可以与按钮类型同时使用。

<DemoBlock source={buttonStatusSource} mainFile="src/button-status.vue">

<ButtonStatusDemo client:only="vue" />

</DemoBlock>

## API

### `<button>` Props

| 参数名    | 描述                                                                                                                                              | 类型                                             |    默认值     |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | :-----------: |
| type      | 按钮的类型，分为五种：次要按钮、主要按钮、虚框按钮、线性按钮、文字按钮。                                                                          | `ButtonTypes`                                    | `'secondary'` |
| shape     | 按钮的形状                                                                                                                                        | `BorderShape`                                    |      `-`      |
| status    | 按钮的状态                                                                                                                                        | `'normal' \| 'warning' \| 'success' \| 'danger'` |  `'normal'`   |
| size      | 按钮的尺寸                                                                                                                                        | `'mini' \| 'small' \| 'medium' \| 'large'`       |  `'medium'`   |
| long      | 按钮的宽度是否随容器自适应。                                                                                                                      | `boolean`                                        |    `false`    |
| loading   | 按钮是否为加载中状态                                                                                                                              | `boolean`                                        |    `false`    |
| disabled  | 按钮是否禁用                                                                                                                                      | `boolean`                                        |    `false`    |
| html-type | 设置 `button` 的原生 `type` 属性，可选值参考 [HTML标准](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#attr-type "_blank")      | `HTMLButtonElement['type']`                      |  `'button'`   |
| autofocus | 设置 `button` 的原生 `autofocus` 属性，可选值参考 [HTML标准](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#attr-type "_blank") | `boolean`                                        |    `false`    |
| href      | 设置跳转链接。设置此属性时，按钮渲染为a标签。                                                                                                     | `string`                                         |      `-`      |

### `<button>` Events

| 事件名 | 描述           | 参数             |
| ------ | -------------- | ---------------- |
| click  | 点击按钮时触发 | ev: `MouseEvent` |

### `<button>` Slots

| 插槽名 | 描述 | 参数 |
| ------ | :--: | ---- |
| icon   | 图标 | -    |

### `<button-group>` Props

| 参数名   | 描述                                                                     | 类型                                             | 默认值  |
| -------- | ------------------------------------------------------------------------ | ------------------------------------------------ | :-----: |
| type     | 按钮的类型，分为五种：次要按钮、主要按钮、虚框按钮、线性按钮、文字按钮。 | `ButtonTypes`                                    |   `-`   |
| status   | 按钮的状态                                                               | `'normal' \| 'warning' \| 'success' \| 'danger'` |   `-`   |
| shape    | 按钮的形状                                                               | `BorderShape`                                    |   `-`   |
| size     | 按钮的尺寸                                                               | `'mini' \| 'small' \| 'medium' \| 'large'`       |   `-`   |
| disabled | 全部子按钮是否禁用                                                       | `boolean`                                        | `false` |
