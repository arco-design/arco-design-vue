---
title: 时间轴 Timeline
description: 按照时间顺序或倒序规则的展示信息内容。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import TimelineBasicDemo from '../../../../components/generated/timeline/basic.vue';
import timelineBasicSource from '../../../../components/generated/timeline/basic.vue?raw';
import TimelineCustomDemo from '../../../../components/generated/timeline/custom.vue';
import timelineCustomSource from '../../../../components/generated/timeline/custom.vue?raw';
import TimelineDirectionDemo from '../../../../components/generated/timeline/direction.vue';
import timelineDirectionSource from '../../../../components/generated/timeline/direction.vue?raw';
import TimelineDotDemo from '../../../../components/generated/timeline/dot.vue';
import timelineDotSource from '../../../../components/generated/timeline/dot.vue?raw';
import TimelineIconDemo from '../../../../components/generated/timeline/icon.vue';
import timelineIconSource from '../../../../components/generated/timeline/icon.vue?raw';
import TimelineLabelDemo from '../../../../components/generated/timeline/label.vue';
import timelineLabelSource from '../../../../components/generated/timeline/label.vue?raw';
import TimelineModeDemo from '../../../../components/generated/timeline/mode.vue';
import timelineModeSource from '../../../../components/generated/timeline/mode.vue?raw';
import TimelinePendingDemo from '../../../../components/generated/timeline/pending.vue';
import timelinePendingSource from '../../../../components/generated/timeline/pending.vue?raw';
import TimelineTypeDemo from '../../../../components/generated/timeline/type.vue';
import timelineTypeSource from '../../../../components/generated/timeline/type.vue?raw';
import TimelineVerticalDemo from '../../../../components/generated/timeline/vertical.vue';
import timelineVerticalSource from '../../../../components/generated/timeline/vertical.vue?raw';

### 基本用法

基本使用

<DemoBlock source={timelineBasicSource} mainFile="src/timeline-basic.vue">

<TimelineBasicDemo client:only="vue" />

</DemoBlock>

### 自定义标签

可以通过 `label` 插槽自定义标签

<DemoBlock source={timelineCustomSource} mainFile="src/timeline-custom.vue">

<TimelineCustomDemo client:only="vue" />

</DemoBlock>

### 横向时间轴

可以通过 `direction` 设置展示横向时间轴

<DemoBlock source={timelineDirectionSource} mainFile="src/timeline-direction.vue">

<TimelineDirectionDemo client:only="vue" />

</DemoBlock>

### 自定义节点

可以通过属性 `dotColor`, `dotType` 设置节点的颜色以及节点类型。同时可通过 `dot` 直接传入 DOM 自定义节点样式。优先级高于 `dotColor` 和 `dotType`

<DemoBlock source={timelineDotSource} mainFile="src/timeline-dot.vue">

<TimelineDotDemo client:only="vue" />

</DemoBlock>

### 自定义节点内容

自定义节点内容

<DemoBlock source={timelineIconSource} mainFile="src/timeline-icon.vue">

<TimelineIconDemo client:only="vue" />

</DemoBlock>

### 标签文本位置

通过 `labelPosition` 可以设置标签文本的位置。

<DemoBlock source={timelineLabelSource} mainFile="src/timeline-label.vue">

<TimelineLabelDemo client:only="vue" />

</DemoBlock>

### 时间轴展示类型

设置 `mode=alternate`时将会交替展示内容。同时可以通过设置 `TimelineItem` 的 `positon`属性控制时间轴节点的位置.

<DemoBlock source={timelineModeSource} mainFile="src/timeline-mode.vue">

<TimelineModeDemo client:only="vue" />

</DemoBlock>

### 幽灵节点

当任务状态正在发生，还在记录过程中，可用幽灵节点来表示当前的时间节点，通过`slot#pending-dot`定制其轴点。

<DemoBlock source={timelinePendingSource} mainFile="src/timeline-pending.vue">

<TimelinePendingDemo client:only="vue" />

</DemoBlock>

### 自定义轴线样式

自定义轴线的示例。

<DemoBlock source={timelineTypeSource} mainFile="src/timeline-type.vue">

<TimelineTypeDemo client:only="vue" />

</DemoBlock>

### 纵向时间轴

竖直方向的时间轴。

<DemoBlock source={timelineVerticalSource} mainFile="src/timeline-vertical.vue">

<TimelineVerticalDemo client:only="vue" />

</DemoBlock>

## API

### `<timeline>` Props

| 参数名 | 描述 | 类型 | 默认值 |
| --- | --- | --- | :-: |
| reverse | 是否倒序 | `boolean` | `false` |
| direction | 时间轴方向 | `'horizontal' \| 'vertical'` | `'vertical'` |
| mode | 时间轴的展示类型：时间轴在左侧，时间轴在右侧, 交替出现。 | `'left' \| 'right' \| 'top' \| 'bottom' \| 'alternate'` | `'left'` |
| pending | 是否展示幽灵节点，设置为 true 时候只展示幽灵节点。传入ReactNode时，会作为节点内容展示。 | `boolean\|string` | `-` |
| label-position | 设置标签文本的位置 | `'relative' \| 'same'` | `'same'` |

### `<timeline>` Slots

| 插槽名 |   描述   | 参数 |
| ------ | :------: | ---- |
| dot    | 幽灵节点 | -    |

### `<timeline-item>` Props

| 参数名     | 描述                         | 类型                              |  默认值   |
| ---------- | ---------------------------- | --------------------------------- | :-------: |
| dot-color  | 节点颜色                     | `string`                          |    `-`    |
| dot-type   | 节点类型：空心圆/实心圆      | `'hollow' \| 'solid'`             | `'solid'` |
| line-type  | 时间轴类型：实线/虚线/点状线 | `'solid' \| 'dashed' \| 'dotted'` | `'solid'` |
| line-color | 时间轴颜色                   | `string`                          |    `-`    |
| label      | 标签文本                     | `string`                          |    `-`    |
| position   | Item 位置                    | `PositionType`                    |    `-`    |

### `<timeline-item>` Slots

| 插槽名 |    描述    | 参数 | 版本   |
| ------ | :--------: | ---- | :----- |
| dot    | 自定义节点 | -    |        |
| label  | 自定义标签 | -    | 2.50.0 |
