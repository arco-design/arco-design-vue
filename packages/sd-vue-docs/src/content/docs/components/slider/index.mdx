---
title: 滑动输入条 Slider
description: 滑动型输入器，展示当前值和可选范围。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import SliderBasicDemo from '../../../../components/generated/slider/basic.vue';
import sliderBasicSource from '../../../../components/generated/slider/basic.vue?raw';
import SliderDisabledDemo from '../../../../components/generated/slider/disabled.vue';
import sliderDisabledSource from '../../../../components/generated/slider/disabled.vue?raw';
import SliderInputDemo from '../../../../components/generated/slider/input.vue';
import sliderInputSource from '../../../../components/generated/slider/input.vue?raw';
import SliderMarksDemo from '../../../../components/generated/slider/marks.vue';
import sliderMarksSource from '../../../../components/generated/slider/marks.vue?raw';
import SliderRangeDemo from '../../../../components/generated/slider/range.vue';
import sliderRangeSource from '../../../../components/generated/slider/range.vue?raw';
import SliderStepDemo from '../../../../components/generated/slider/step.vue';
import sliderStepSource from '../../../../components/generated/slider/step.vue?raw';
import SliderTooltipDemo from '../../../../components/generated/slider/tooltip.vue';
import sliderTooltipSource from '../../../../components/generated/slider/tooltip.vue?raw';
import SliderVerticalDemo from '../../../../components/generated/slider/vertical.vue';
import sliderVerticalSource from '../../../../components/generated/slider/vertical.vue?raw';

### 基本用法

滑动输入条的基本用法。

<DemoBlock source={sliderBasicSource} mainFile="src/slider-basic.vue">

<SliderBasicDemo client:only="vue" />

</DemoBlock>

### 禁用状态

禁用滑动输入条。

<DemoBlock source={sliderDisabledSource} mainFile="src/slider-disabled.vue">

<SliderDisabledDemo client:only="vue" />

</DemoBlock>

### 显示输入框

当设置 `show-input` 时，将显示输入框。

<DemoBlock source={sliderInputSource} mainFile="src/slider-input.vue">

<SliderInputDemo client:only="vue" />

</DemoBlock>

### 添加文本标签

通过设置 `marks` 可以添加文本标签。

<DemoBlock source={sliderMarksSource} mainFile="src/slider-marks.vue">

<SliderMarksDemo client:only="vue" />

</DemoBlock>

### 范围选择

通过设置 `range` 可开启范围选择，此时 `modelValue` 为数组。

<DemoBlock source={sliderRangeSource} mainFile="src/slider-range.vue">

<SliderRangeDemo client:only="vue" />

</DemoBlock>

### 设置步长

通过 `step` 设置步长，默认步长为 1。建议设置能够被 `max-min` 整除的值，否则会出现可选最大值小于 `max` 的情况。当设置 `show-ticks` 时，显示步长刻度线。

<DemoBlock source={sliderStepSource} mainFile="src/slider-step.vue">

<SliderStepDemo client:only="vue" />

</DemoBlock>

### 自定义提示

通过设置 `format-tooltip` 可以自定义提示文字。

<DemoBlock source={sliderTooltipSource} mainFile="src/slider-tooltip.vue">

<SliderTooltipDemo client:only="vue" />

</DemoBlock>

### 竖直滑动条

设置 `direction="vertical"` ，将会显示竖直的滑动条。

<DemoBlock source={sliderVerticalSource} mainFile="src/slider-vertical.vue">

<SliderVerticalDemo client:only="vue" />

</DemoBlock>

## API

### `<slider>` Props

| 参数名 | 描述 | 类型 | 默认值 | 版本 |
| --- | --- | --- | :-: | :-- |
| model-value **(v-model)** | 绑定值 | `number \| [number, number]` | `-` |  |
| default-value | 默认值（非受控状态） | `number \| [number, number]` | `0` |  |
| step | 滑动的步长 | `number` | `1` |  |
| min | 滑动范围的最小值 | `number` | `0` |  |
| marks | 设置显示的标签 | `Record<number, string>` | `-` |  |
| max | 滑动范围的最大值 | `number` | `100` |  |
| direction | 滑动输入条的方向 | `Direction` | `'horizontal'` |  |
| disabled | 是否禁用 | `boolean` | `false` |  |
| show-ticks | 是否显示刻度线 | `boolean` | `false` |  |
| show-input | 是否显示输入框 | `boolean` | `false` |  |
| range | 是否开启范围选择 | `boolean` | `false` |  |
| show-tooltip | 是否显示tooltip | `boolean` | `true` | 2.42.0 |

### `<slider>` Events

| 事件名 | 描述         | 参数                                |
| ------ | ------------ | ----------------------------------- |
| change | 值改变时触发 | value: `number \| [number, number]` |
