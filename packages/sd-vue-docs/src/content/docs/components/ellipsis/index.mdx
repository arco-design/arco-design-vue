---
title: 文本省略 Ellipsis
description: 在有限空间内展示较长文本，并在内容被截断时提供提示或展开能力。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import EllipsisBasicDemo from '../../../../components/generated/ellipsis/basic.vue';
export const ellipsisBasicSource =
  '<template>\n  <sd-ellipsis style="max-width: 240px;">\n    A design is a plan or specification for the construction of an object or system.\n  </sd-ellipsis>\n</template>';
import EllipsisLineClampDemo from '../../../../components/generated/ellipsis/line-clamp.vue';
export const ellipsisLineClampSource =
  '<template>\n  <sd-ellipsis :line-clamp="2" style="max-width: 320px;">\n    A design is a plan or specification for the construction of an object or system or for the implementation of an activity or process, or the result of that plan or specification in the form of a prototype, product or process.\n  </sd-ellipsis>\n</template>';
import EllipsisExpandTriggerDemo from '../../../../components/generated/ellipsis/expand-trigger.vue';
export const ellipsisExpandTriggerSource =
  '<template>\n  <sd-ellipsis expand-trigger="click" :tooltip="false" style="max-width: 280px;">\n    Click the truncated content to expand it and click again to collapse it. This interaction is useful when the layout space is limited but the full copy still needs to remain accessible.\n  </sd-ellipsis>\n</template>';
import EllipsisCustomTooltipDemo from '../../../../components/generated/ellipsis/custom-tooltip.vue';
export const ellipsisCustomTooltipSource =
  '<template>\n  <sd-ellipsis :tooltip="{ position: \'bottom\', mini: true }" style="display: block; max-width: 260px;">\n    Hover the truncated content to show a custom tooltip body rendered by the tooltip slot.\n    <template #tooltip>\n      <div>\n        <strong>Full content</strong>\n        <div style="margin-top: 4px;">Hover the truncated content to show a custom tooltip body rendered by the tooltip slot.</div>\n      </div>\n    </template>\n  </sd-ellipsis>\n</template>';
import PerformantEllipsisDemo from '../../../../components/generated/ellipsis/performant-ellipsis.vue';
export const performantEllipsisSource =
  '<template>\n  <sd-performant-ellipsis style="max-width: 240px;">\n    In large lists, performant ellipsis avoids eagerly wiring tooltip and resize logic until the user actually interacts with the truncated content.\n  </sd-performant-ellipsis>\n</template>';

### 基本用法

默认按单行省略处理，适合表格单元格、列表标题等横向空间有限的场景。

<DemoBlock source={ellipsisBasicSource} mainFile="src/ellipsis-basic.vue">

<EllipsisBasicDemo client:only="vue" />

</DemoBlock>

### 多行省略

通过 `line-clamp` 指定最大展示行数，超出部分将按多行截断。

<DemoBlock source={ellipsisLineClampSource} mainFile="src/ellipsis-line-clamp.vue">

<EllipsisLineClampDemo client:only="vue" />

</DemoBlock>

### 点击展开

通过 `expand-trigger="click"` 开启点击展开与收起。关闭 tooltip 后，会退回到原生 `title` 提示。

<DemoBlock source={ellipsisExpandTriggerSource} mainFile="src/ellipsis-expand-trigger.vue">

<EllipsisExpandTriggerDemo client:only="vue" />

</DemoBlock>

### 自定义提示内容

`tooltip` 属性可传入 Tooltip 配置，`tooltip` 插槽可自定义完整提示内容。

<DemoBlock source={ellipsisCustomTooltipSource} mainFile="src/ellipsis-custom-tooltip.vue">

<EllipsisCustomTooltipDemo client:only="vue" />

</DemoBlock>

### 高性能渲染

`sd-performant-ellipsis` 适合大量列表项同时渲染的场景。它会先以纯 CSS 省略形式输出，等用户真正交互后再切换到完整的 `sd-ellipsis` 能力，因此首屏渲染开销更低，但内部节点会在激活时重新挂载。

<DemoBlock source={performantEllipsisSource} mainFile="src/ellipsis-performant.vue">

<PerformantEllipsisDemo client:only="vue" />

</DemoBlock>

## 从 Naive UI 迁移

如果你正在把 `naive-ui` 的 `NEllipsis` 或 `NPerformantEllipsis` 迁移到当前组件库，可以直接参考[迁移文档](guides/naive-ui-migration/#文本省略-ellipsis)。

当前这次迁移已经对齐了以下能力：

- 单行省略与多行省略
- 点击展开
- `tooltip` 插槽自定义提示内容
- 高性能渲染版本 `sd-performant-ellipsis`

常见替换关系如下：

| Naive UI                | SD Design Vue            |
| ----------------------- | ------------------------ |
| `n-ellipsis`            | `sd-ellipsis`            |
| `n-performant-ellipsis` | `sd-performant-ellipsis` |

如果业务代码里需要显式导入组件，也可以直接导入 `Ellipsis` 和 `PerformantEllipsis` 组件使用。

## API

### `<ellipsis>` / `<performant-ellipsis>` Props

| 参数名 | 描述 | 类型 | 默认值 |
| --- | --- | --- | :-: |
| line-clamp | 最大显示行数。不传时为单行省略 | `number \| string` | `-` |
| expand-trigger | 展开的触发方式 | `'click'` | `-` |
| tooltip | 省略时是否展示提示。可传入 Tooltip 属性 | `boolean \| EllipsisTooltipProps` | `true` |

### `<ellipsis>` / `<performant-ellipsis>` Slots

| 插槽名  | 描述           | 参数 |
| ------- | -------------- | ---- |
| default | 默认内容       | -    |
| tooltip | 自定义提示内容 | -    |
