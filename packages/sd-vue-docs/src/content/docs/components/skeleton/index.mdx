---
title: 骨架屏 Skeleton
description: 将加载中的数据用灰色占位。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import SkeletonAnimationDemo from '../../../../components/generated/skeleton/animation.vue';
import skeletonAnimationSource from '../../../../components/generated/skeleton/animation.vue?raw';
import SkeletonBasicDemo from '../../../../components/generated/skeleton/basic.vue';
import skeletonBasicSource from '../../../../components/generated/skeleton/basic.vue?raw';
import SkeletonTypeDemo from '../../../../components/generated/skeleton/type.vue';
import skeletonTypeSource from '../../../../components/generated/skeleton/type.vue?raw';

### 动画

通过设置 `animation` 属性，让骨架屏显示动画效果。

<DemoBlock source={skeletonAnimationSource} mainFile="src/skeleton-animation.vue">

<SkeletonAnimationDemo client:only="vue" />

</DemoBlock>

### 基本用法

骨架屏组件提供 `<sd-skeleton-line>` 和 `<sd-skeleton-shape>` 两种组件，用户可根据需要组合使用。

<DemoBlock source={skeletonBasicSource} mainFile="src/skeleton-basic.vue">

<SkeletonBasicDemo client:only="vue" />

</DemoBlock>

### 图形骨架屏

图形骨架屏分为 `square` - **正方形（默认）**、 `circle` - **圆形**两种形状，并提供 `small`、`medium`、`large` 三种尺寸。

<DemoBlock source={skeletonTypeSource} mainFile="src/skeleton-type.vue">

<SkeletonTypeDemo client:only="vue" />

</DemoBlock>

## API

### `<skeleton>` Props

| 参数名    | 描述                         | 类型      | 默认值  |
| --------- | ---------------------------- | --------- | :-----: |
| loading   | 是否展示骨架屏（加载中状态） | `boolean` | `true`  |
| animation | 是否开启骨架屏动画           | `boolean` | `false` |

### `<skeleton-line>` Props

| 参数名       | 描述             | 类型                      | 默认值 |
| ------------ | ---------------- | ------------------------- | :----: |
| rows         | 展示的行数       | `number`                  |  `1`   |
| widths       | 线型骨架的宽度   | `Array<number \| string>` |  `[]`  |
| line-height  | 线型骨架的行高   | `number`                  |  `20`  |
| line-spacing | 线型骨架的行间距 | `number`                  |  `15`  |

### `<skeleton-shape>` Props

| 参数名 | 描述           | 类型                             |   默认值   |
| ------ | -------------- | -------------------------------- | :--------: |
| shape  | 图形骨架的形状 | `'square' \| 'circle'`           | `'square'` |
| size   | 图形骨架的大小 | `'small' \| 'medium' \| 'large'` | `'medium'` |
