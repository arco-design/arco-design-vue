---
title: 徽标数 Badge
description: 一般出现在图标或文字的右上角。提供及时、重要的信息提示。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import BadgeAloneDemo from '../../../../components/generated/badge/alone.vue';
import badgeAloneSource from '../../../../components/generated/badge/alone.vue?raw';
import BadgeBasicDemo from '../../../../components/generated/badge/basic.vue';
import badgeBasicSource from '../../../../components/generated/badge/basic.vue?raw';
import BadgeColorDemo from '../../../../components/generated/badge/color.vue';
import badgeColorSource from '../../../../components/generated/badge/color.vue?raw';
import BadgeDotDemo from '../../../../components/generated/badge/dot.vue';
import badgeDotSource from '../../../../components/generated/badge/dot.vue?raw';
import BadgeMaxDemo from '../../../../components/generated/badge/max.vue';
import badgeMaxSource from '../../../../components/generated/badge/max.vue?raw';
import BadgeStatusDemo from '../../../../components/generated/badge/status.vue';
import badgeStatusSource from '../../../../components/generated/badge/status.vue?raw';
import BadgeTextDemo from '../../../../components/generated/badge/text.vue';
import badgeTextSource from '../../../../components/generated/badge/text.vue?raw';

### 独立使用

`default slot` 为空时，将会独立展示徽标。

<DemoBlock source={badgeAloneSource} mainFile="src/badge-alone.vue">

<BadgeAloneDemo client:only="vue" />

</DemoBlock>

### 基本用法

基本用法。只需指定 `count`或者 `content slot`，即可显示徽标。

<DemoBlock source={badgeBasicSource} mainFile="src/badge-basic.vue">

<BadgeBasicDemo client:only="vue" />

</DemoBlock>

### 颜色

我们提供多种预设色彩的徽标样式。如果预设值不能满足你的需求，`color` 字段也可以设置自定义色值。

<DemoBlock source={badgeColorSource} mainFile="src/badge-color.vue">

<BadgeColorDemo client:only="vue" />

</DemoBlock>

### 小红点

设置 `dot`，即可只显示小红点而不显示数字。`count > 0` 时才显示。

<DemoBlock source={badgeDotSource} mainFile="src/badge-dot.vue">

<BadgeDotDemo client:only="vue" />

</DemoBlock>

### 最大值

设置 `max-count`，可以限制最大显示的徽标数值，超过将会加 `+` 后缀。`max-count` 默认为 `99`。

<DemoBlock source={badgeMaxSource} mainFile="src/badge-max.vue">

<BadgeMaxDemo client:only="vue" />

</DemoBlock>

### 状态点

设置 `status`，可以得到不同的状态点。`normal - 正常` `processing - 进行中` `success - 成功` `warning - 提醒` `danger - 危险`。

<DemoBlock source={badgeStatusSource} mainFile="src/badge-status.vue">

<BadgeStatusDemo client:only="vue" />

</DemoBlock>

### 文本内容

设置 `text`，可设置自定义提示内容。

<DemoBlock source={badgeTextSource} mainFile="src/badge-text.vue">

<BadgeTextDemo client:only="vue" />

</DemoBlock>

## API

### `<badge>` Props

| 参数名 | 描述 | 类型 | 默认值 |
| --- | --- | --- | :-: |
| text | 自定义提示内容 | `string` | `-` |
| dot | 显示为小红点 | `boolean` | `false` |
| dot-style | 徽标的样式 | `object` | `-` |
| max-count | 徽标最大显示数值，如果count超过这个数值会显示为maxCount | `number` | `99` |
| offset | 设置徽标位置的偏移 | `number[]` | `[]` |
| color | 内置的一些颜色 | `ColorType \| string` | `-` |
| status | 徽标的状态类型 | `'normal' \| 'processing' \| 'success' \| 'warning' \| 'danger'` | `-` |
| count | 徽标显示的数字 | `number` | `-` |
