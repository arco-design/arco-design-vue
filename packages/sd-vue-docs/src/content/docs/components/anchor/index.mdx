---
title: 锚点 Anchor
description: 通过锚点可快速找到信息内容在当前页面的位置。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import AnchorAffixDemo from '../../../../components/generated/anchor/affix.vue';
import anchorAffixSource from '../../../../components/generated/anchor/affix.vue?raw';
import AnchorBasicDemo from '../../../../components/generated/anchor/basic.vue';
import anchorBasicSource from '../../../../components/generated/anchor/basic.vue?raw';
import AnchorBoundaryDemo from '../../../../components/generated/anchor/boundary.vue';
import anchorBoundarySource from '../../../../components/generated/anchor/boundary.vue?raw';
import AnchorHashDemo from '../../../../components/generated/anchor/hash.vue';
import anchorHashSource from '../../../../components/generated/anchor/hash.vue?raw';
import AnchorLineLessDemo from '../../../../components/generated/anchor/line-less.vue';
import anchorLineLessSource from '../../../../components/generated/anchor/line-less.vue?raw';

### 固钉位置

使用 `affix` 组件可以让锚点固定在页面之内。

<DemoBlock source={anchorAffixSource} mainFile="src/anchor-affix.vue">

<AnchorAffixDemo client:only="vue" />

</DemoBlock>

### 基本用法

锚点的基础用法

<DemoBlock source={anchorBasicSource} mainFile="src/anchor-basic.vue">

<AnchorBasicDemo client:only="vue" />

</DemoBlock>

### 锚点滚动偏移量

可以设置 `boundary` 来定制锚点滚动偏移量。

<DemoBlock source={anchorBoundarySource} mainFile="src/anchor-boundary.vue">

<AnchorBoundaryDemo client:only="vue" />

</DemoBlock>

### 是否改变hash

可以设置点击锚点而不改变浏览器历史。

<DemoBlock source={anchorHashSource} mainFile="src/anchor-hash.vue">

<AnchorHashDemo client:only="vue" />

</DemoBlock>

### 无轴线模式

设置 `line-less` 时，可以使用无左侧轴线的锚点样式。

<DemoBlock source={anchorLineLessSource} mainFile="src/anchor-line-less.vue">

<AnchorLineLessDemo client:only="vue" />

</DemoBlock>

## API

### `<anchor>` Props

| 参数名 | 描述 | 类型 | 默认值 |
| --- | --- | --- | :-: |
| boundary | 滚动边界值，设置该值为数字后，将会在距离滚动容器 `boundary` 距离时停止滚动。 | `'start' \| 'end' \| 'center' \| 'nearest' \| number` | `'start'` |
| line-less | 是否显示左侧轴线 | `boolean` | `false` |
| scroll-container | 滚动容器 | `string \| HTMLElement \| Window` | `-` |
| change-hash | 是否改变hash。设置为 `false` 时点击锚点不会改变页面的 hash | `boolean` | `true` |
| smooth | 是否使用平滑滚动 | `boolean` | `true` |

### `<anchor>` Events

| 事件名 | 描述               | 参数                                               |
| ------ | ------------------ | -------------------------------------------------- |
| select | 用户点击链接时触发 | hash: `string \| undefined`<br />preHash: `string` |
| change | 链接发生改变时触发 | hash: `string`                                     |

### `<anchor-link>` Props

| 参数名 | 描述               | 类型     | 默认值 |
| ------ | ------------------ | -------- | :----: |
| title  | 锚点链接的文本内容 | `string` |  `-`   |
| href   | 锚点链接的地址     | `string` |  `-`   |
