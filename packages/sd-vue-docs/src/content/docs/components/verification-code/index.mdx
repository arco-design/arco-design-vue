---
title: 验证码输入 VerificationCode
description: 验证码输入组件
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import VerificationCodeBasicDemo from '../../../../components/generated/verification-code/basic.vue';
import verificationCodeBasicSource from '../../../../components/generated/verification-code/basic.vue?raw';
import VerificationCodeFormDemo from '../../../../components/generated/verification-code/form.vue';
import verificationCodeFormSource from '../../../../components/generated/verification-code/form.vue?raw';
import VerificationCodeFormatterDemo from '../../../../components/generated/verification-code/formatter.vue';
import verificationCodeFormatterSource from '../../../../components/generated/verification-code/formatter.vue?raw';
import VerificationCodeMaskedDemo from '../../../../components/generated/verification-code/masked.vue';
import verificationCodeMaskedSource from '../../../../components/generated/verification-code/masked.vue?raw';
import VerificationCodeSeparatorDemo from '../../../../components/generated/verification-code/separator.vue';
import verificationCodeSeparatorSource from '../../../../components/generated/verification-code/separator.vue?raw';
import VerificationCodeStatusDemo from '../../../../components/generated/verification-code/status.vue';
import verificationCodeStatusSource from '../../../../components/generated/verification-code/status.vue?raw';

### 基本使用

验证码输入框的基本用法。

<DemoBlock source={verificationCodeBasicSource} mainFile="src/verification-code-basic.vue">

<VerificationCodeBasicDemo client:only="vue" />

</DemoBlock>

### 配合表单使用

配合表单使用实现校验。

<DemoBlock source={verificationCodeFormSource} mainFile="src/verification-code-form.vue">

<VerificationCodeFormDemo client:only="vue" />

</DemoBlock>

### 格式化输入

通过 `formatter` 校验输入。此外，可以返回非布尔类型来将用户输入的字符串为特定的格式。

<DemoBlock source={verificationCodeFormatterSource} mainFile="src/verification-code-formatter.vue">

<VerificationCodeFormatterDemo client:only="vue" />

</DemoBlock>

### 密码模式

指定 `masked = true`可开启密码模式

<DemoBlock source={verificationCodeMaskedSource} mainFile="src/verification-code-masked.vue">

<VerificationCodeMaskedDemo client:only="vue" />

</DemoBlock>

### 自定义分隔符

指定 `separator` 可以自定义渲染分隔符。

<DemoBlock source={verificationCodeSeparatorSource} mainFile="src/verification-code-separator.vue">

<VerificationCodeSeparatorDemo client:only="vue" />

</DemoBlock>

### 不同状态

禁用状态、只读状态、错误状态。

<DemoBlock source={verificationCodeStatusSource} mainFile="src/verification-code-status.vue">

<VerificationCodeStatusDemo client:only="vue" />

</DemoBlock>

## API

### `<verification-code>` Props

| 参数名 | 描述 | 类型 | 默认值 |
| --- | --- | --- | :-: |
| model-value **(v-model)** | 绑定值 | `string` | `-` |
| default-value | 默认值（非受控状态） | `string` | `''` |
| length | 验证码的长度，根据长度渲染对应个数的输入框 | `number` | `6` |
| size | 输入框大小 | `'mini' \| 'small' \| 'medium' \| 'large'` | `'medium'` |
| disabled | 是否禁用 | `boolean` | `false` |
| masked | 是否密码模式 | `boolean` | `false` |
| readonly | 只读 | `boolean` | `false` |
| error | 是否为错误状态 | `boolean` | `false` |
| separator | 分隔符。可在不同索引的输入框后自定义渲染分隔符 | `(index: number, character: string) => VNode` | `-` |
| formatter | 格式化函数，当用户输入值改变时触发 | `(inputValue: string, index: number, value: string) => string \| boolean` | `-` |

### `<verification-code>` Events

| 事件名 | 描述             | 参数                                                       |
| ------ | ---------------- | ---------------------------------------------------------- |
| change | 值发生改变时触发 | value: `string`                                            |
| finish | 填充完成时触发   | value: `string`                                            |
| input  | 输入时触发       | inputValue: `string`<br />index: `number`<br />ev: `Event` |
