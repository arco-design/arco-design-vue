---
title: 页头 PageHeader
description: 页头位于页容器顶部，起到了内容概览和引导页级操作的作用。包括面包屑、标题等内容。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import PageHeaderBasicDemo from '../../../../components/generated/page-header/basic.vue';
import pageHeaderBasicSource from '../../../../components/generated/page-header/basic.vue?raw';
import PageHeaderBreadcrumbDemo from '../../../../components/generated/page-header/breadcrumb.vue';
import pageHeaderBreadcrumbSource from '../../../../components/generated/page-header/breadcrumb.vue?raw';
import PageHeaderContentDemo from '../../../../components/generated/page-header/content.vue';
import pageHeaderContentSource from '../../../../components/generated/page-header/content.vue?raw';
import PageHeaderTransparentDemo from '../../../../components/generated/page-header/transparent.vue';
import pageHeaderTransparentSource from '../../../../components/generated/page-header/transparent.vue?raw';

### 基本用法

基础页头，适合使用在需要简单描述的场景。默认是没有底色的。

<DemoBlock source={pageHeaderBasicSource} mainFile="src/page-header-basic.vue">

<PageHeaderBasicDemo client:only="vue" />

</DemoBlock>

### 带有面包屑

在页头中展示面包屑。

<DemoBlock source={pageHeaderBreadcrumbSource} mainFile="src/page-header-breadcrumb.vue">

<PageHeaderBreadcrumbDemo client:only="vue" />

</DemoBlock>

### 组合示例

页头的完整示例。

<DemoBlock source={pageHeaderContentSource} mainFile="src/page-header-content.vue">

<PageHeaderContentDemo client:only="vue" />

</DemoBlock>

### 透明底色

默认是没有底色的，如果有需要可以通过`style`或类名设置不同底色。

<DemoBlock source={pageHeaderTransparentSource} mainFile="src/page-header-transparent.vue">

<PageHeaderTransparentDemo client:only="vue" />

</DemoBlock>

## API

### `<page-header>` Props

| 参数名    | 描述             | 类型      | 默认值 |
| --------- | ---------------- | --------- | :----: |
| title     | 页头的主标题     | `string`  |  `-`   |
| subtitle  | 页头的次标题     | `string`  |  `-`   |
| show-back | 是否显示返回按钮 | `boolean` | `true` |

### `<page-header>` Events

| 事件名 | 描述               | 参数           |
| ------ | ------------------ | -------------- |
| back   | 点击返回按钮时触发 | event: `Event` |

### `<page-header>` Slots

| 插槽名     |      描述      | 参数 | 版本   |
| ---------- | :------------: | ---- | :----- |
| breadcrumb |     面包屑     | -    |        |
| back-icon  |    返回按钮    | -    | 2.36.0 |
| title      |     主标题     | -    |        |
| subtitle   |     次标题     | -    |        |
| extra      | 额外的展示内容 | -    |        |
